# Runtime — the analysis engine is pure standard library.
flask>=3.0.0
flask-cors>=4.0.0

# Tests
pytest>=8.0.0

# Optional extras, both feature-detected at runtime:
#   anthropic>=0.40.0     # the written Claude summary on the Savings tab
#   plaid-python>=20.0.0  # live bank sync instead of CSV exports
